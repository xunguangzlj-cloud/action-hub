# 行动中枢使用说明

适用版本：安卓 1.1.1，以及包含“一句话记录”的新版网页。更新日期：2026-09-03。

这是一个面向个人的工作记录与行动管理工具。内容先保存在当前设备；配置自己的国内云同步服务后，电脑和手机可以交换记录。文件登记只保存文件名、类型和手动填写的位置，不保存原文件。

## 1. 先用起来

1. 安卓手机安装本发布包中的 `行动中枢-Android-1.1.1.apk`。升级旧版时，先导出备份，再直接覆盖安装，不要先卸载或清除数据。
2. Windows 电脑使用已有的工作台网页地址。安卓也可使用网页，但网页版与安装版的本机数据彼此独立。
3. 打开“收集箱”，在“一句话记录”输入栏写下一件事，点击“记录”。记录立即保存到本机，并打开固定的“一句话记录”文件夹。
4. 有时间时，点击记录旁的“整理”，选择它应归入的状态、文件夹与优先级，点击“保存任务”。
5. 需要电脑、手机互通时，按第 8 节配置并执行同步。

没有配置云服务，也可以在本机离线记录。安装包不是云账户；新下载者不能直接接入原使用者的个人云数据。现有用户继续使用自己的工作台网址与同步配置，不必因更新而重新生成密钥。

## 2. 四个主要页面

| 页面 | 用途 | 注意 |
| --- | --- | --- |
| 收集箱 | 登记资料位置、归类文件线索、快速记下一句话 | 不自动进入行动计划的“明确”阶段 |
| 行动计划 | 通过“收集 → 明确 → 行动”整理要做的事 | 阶段之间由你手动移动，不由系统替你判断 |
| 今日行动 | 查看计划日期为今天的未完成记录 | 不会自动把昨天未完成的事项顺延到今天 |
| 完成记录 | 查看已完成记录、恢复未完成状态或批量删除 | 删除不同于完成；删除没有回收站入口 |

“今日行动”按计划日期汇总普通记录，并不只限于“行动”文件夹。“一句话记录”是独立区域，未移出时不会进入今日行动。

## 3. 收集箱：登记资料与一句话记录

### 一句话记录

在搜索栏下方的“一句话记录”栏输入内容，然后点击“记录”，或在中文输入完成后按回车。

- 不需要填写位置、日期或优先级。
- 保存后输入框清空，可连续记录。
- 记录进入固定的“一句话记录”文件夹，不会因为当前选中其他文件夹而改变去向。
- 内容不出现在“全部”“优先级”“未归类”“未明确位置”“今日行动”等普通汇总中；全局搜索仍可找到它。
- 固定文件夹不能删除、改名、移动，也不能建立子文件夹。
- 点击“整理”，选择其他目标文件夹并保存后，该记录才进入相应的普通分类。

不要把“一句话记录”当作已经规划好的任务清单。建议先整理移出，再执行、完成；直接在其中标记完成的条目仍留在这个独立文件夹，不会进入普通“完成记录”。

### 手动登记

点击“＋ 记录一件事”，填写“内容”。位置可以留空；普通整理表单需要由你选择优先级。选择状态与文件夹后，点击“保存任务”。

位置是寻找原始资料的线索，例如：`企业微信 / 项目群 / 9月3日文件`、`微信 / 某联系人`、`D:\资料\报价`。填写过的位置会在输入框下方提供候选；输入关键词可筛选，点击候选即可填入。候选来自当前设备已有记录，同步后也可利用另一端带来的位置。

### 拖入或选择文件

电脑上可把文件拖到收集箱的拖放区域，也可以直接拖到实际的文件夹图标上。手机可点击“选择文件”，能否打开文件选择器以设备支持为准。

- 每个文件建立一条记录，只登记文件名、类型。
- 文件内容不会被上传；原文件不移动、不删除、不复制到应用。
- 完整路径不会自动读取。点击“整理”手动填写位置，留空也能保留记录。
- 同步后另一设备能看到文件线索，但不能因此直接打开电脑上的原文件。
- “全部”“优先级”“未归类”“未明确位置”是筛选入口，不是可以拖入文件的真实目录。

## 4. 行动计划：收集、明确、行动、回顾

顶部流程栏对应行动计划中的三个固定文件夹，以及每周回顾。

1. **收集**：放入需要处理、尚未想清楚的事情。
2. **明确**：把事情写成具体动作，例如将“客户报价”改成“整理三家供应商报价并发给客户”。自行决定优先级、耗时、日期及提醒。
3. **行动**：放入已经清楚、可以执行的事项。需要今天做的，设置计划日期为今天。
4. **回顾**：点击顶部“4 · 回顾”，检查收集、明确、行动、未来两周和近期完成情况，再点击“完成本周回顾”记录时间。

移动步骤：点击条目“整理” → 状态选择“行动计划” → 文件夹选择“收集”“明确”“行动”或它们的子文件夹 → “保存任务”。

“明确”阶段的已有任务可使用“两分钟内已完成”按钮，适用于你已经做完的小事，不是计时器。请先保存需要保留的表单修改，再标记完成。

每周回顾不会替你自动搬动记录，也不包含收集箱的位置整理。请额外查看“一句话记录”和“未明确位置”，决定是否整理。

## 5. 文件夹、搜索和排序

### 文件夹

收集箱和行动计划分别有自己的文件夹。点击“＋ 文件夹”新建；选中普通文件夹后，可用“＋ 子文件夹”继续细分。也可在管理表单中选择“上级文件夹”。

同一个上级文件夹下不能重名。普通文件夹可改名、改变上级或删除；删除时，该层直接存放的记录移到“未归类”，子文件夹上移一级，不会连同记录一起删除。固定流程文件夹和“一句话记录”不能删除或改名。

选中一个文件夹查看的是直接归入该层的记录，不自动展开所有子文件夹的记录。要看子文件夹内容，请点击相应子文件夹，或使用搜索。

### 固定筛选入口

- “全部”：当前页面的普通未完成记录。
- “优先级”：汇总收集箱与行动计划中的普通未完成记录，并按优先级排序。
- “未归类”：当前页面尚未归入实际文件夹的记录。
- “未明确位置”：汇总收集箱与行动计划中位置为空的普通未完成记录。

上述汇总均不包含“一句话记录”中的内容。在行动计划页面，时间筛选也会作用于这些列表。

### 搜索定位

搜索栏可搜索内容、位置、备注、文件名、文件类型和文件夹路径。它是全局搜索，也能找到完成记录和“一句话记录”。输入连续的一段关键词即可；当前不是按多个词智能联想的搜索。

找到目标后点击“定位”，会跳到所属页面与文件夹，并突出显示目标。需要修改时，再点击“整理”。搜索结果是记录，不是单独的空文件夹列表。

若定位后仍看不到，请把“当前可用时间”改为“全部”，排除耗时筛选的影响。搜索结束可点击“清除”，或切换左侧页面。

### 优先级与置顶

优先级始终由你决定：P1 必须完成、P2 应当完成、P3 有空完成、P4 资料或想法。系统不会自动替你设置。

普通列表依次按以下规则排列：置顶在前 → P1 到 P4 → 未设置 → 计划日期较早 → 开始时间较早 → 创建时间较早。没有日期或时间的，同等条件下靠后。

点击“置顶”可突出重要条目，再点“取消置顶”恢复。置顶不等于完成、不改变日期或优先级，也不会让记录脱离所属文件夹。“完成记录”按完成时间从新到旧排列；今日时间轨按开始时间排列。

## 6. 每天安排、保存与完成

1. 点击“整理”，写清内容，并选择优先级。
2. 设置“计划日期”；需要时间块时填写“开始时间”和“预计分钟”。
3. 需要提醒时单独填写“提醒时间”。计划日期、开始时间本身不会自动生成提醒。
4. 点击“保存任务”，回到列表检查记录。
5. 执行完点击条目左侧圆形完成按钮；普通记录从待办列表移到“完成记录”。

“当前可用时间”可按 15、30、60 分钟内筛选行动计划或今日行动。预计耗时只是你填入的计划值，不是自动计时结果。今日统计显示当天计划的总耗时与待完成数量，不会自动限制你的每日工作量。

### 怎么才算保存了

任务表单需要点击“保存任务”，文件夹表单需要点击“保存”。直接关闭或取消表单，不会保存刚填的表单修改。

一句话点击“记录”、文件拖入、置顶、标记完成等操作会直接保存到本机。保存后无需额外点击全局保存按钮。

“本机已保存”只表示当前设备已保存；“同步完成”表示该次设备与云端交换完成，不代表另一台设备已经读取。系统状态提示可能随后被同步状态替换。

### 删除与恢复

普通完成记录可再次点击左侧完成按钮，恢复为未完成。删除条目则需要进入“整理”并点击“删除”。完成记录支持“批量删除” → 勾选或全选 → “删除所选” → 确认。

删除会参与后续同步，其他设备也会收到删除状态。应用没有撤销删除或回收站入口，请在批量删除前备份。不要把云同步当作独立备份。

## 7. 提醒怎么用

提醒是本设备的通知，不是微信、企业微信消息，也不是短信。每台设备需要允许自己的通知权限；接收另一台设备新增的提醒前，应先完成同步并打开应用。

安卓安装版会尝试安排系统本地通知，并请求使用系统提示音。是否有声音仍取决于手机通知权限、通知渠道声音、通知音量、勿扰模式和厂商后台限制。当前版本没有承诺在强制停止、设备重启或系统清理后仍可靠提醒。

网页版使用浏览器通知。需要允许站点通知，并保持页面运行；关闭页面、浏览器休眠或手机系统暂停网页后，不能保证提醒。当前没有服务器定时推送。

请先设一个几分钟后的非重要提醒实测。重要截止时间同时使用系统闹钟或日历。当前原生提醒的修改、取消未覆盖全部已排队通知场景，完成或改期后仍可能收到旧通知；请以应用内任务状态为准。

## 8. 电脑和手机怎样同步

### 首次配置

进入两台设备各自的“数据” → “国内云同步”，填写同一个 HTTPS 云函数地址和同一个同步密钥，点击“保存并测试”。密钥还必须与服务端配置一致。应用只接受至少 24 位密钥，但长度正确不等于与服务端相同。

已在使用同步的用户，更新 APK 后通常沿用本机原配置，不要点击“生成强密钥”替换现有值。新生成密钥并不会自动修改云端配置。网页地址是打开工作台的地址，云函数地址是同步接口地址，二者不要混填。

本发布包不附带任何人的密钥，也不提供多用户公共同步服务。新用户若需联网同步，应单独准备自己的服务，或联系部署者。不要和不相关的人共用一套地址与密钥，否则会访问并合并同一份数据。

### 最稳妥的换设备操作

1. 在设备 A 完成编辑，点击“保存任务”。
2. 在 A 的“数据”中点击“立即同步”，等待显示“同步完成”。
3. 打开设备 B，同样点击“数据” → “立即同步”，等待“同步完成”。
4. 回到对应文件夹检查。需要时清除搜索，并把可用时间设为“全部”。

反方向相同：手机改完先同步手机，再在电脑同步。为避免覆盖，尽量不要同时编辑两端的同一条记录；同步过程中先等它完成，再继续修改。

### 自动同步的实际规则

- 本机有修改且联网时，停止连续操作约 1 分钟后触发同步。
- 应用启动时，没有待同步修改且距离本机上次同步不足 1 小时，会跳过自动检查；超过约 1 小时会尝试检查。
- 网络从离线恢复时，会按是否存在本机修改及上次同步时间安排同步。
- 当前不是实时推送，也没有持续轮询。另一台一直开着、没有操作的设备，不保证立刻显示新内容。
- 自动计时依赖应用仍在运行。刚保存便彻底关闭应用，不能指望后台一定完成上传。

因此，切换设备前后各点一次“立即同步”最直观。只在电脑点一次，不代表手机已拉取。

### 同步什么、不同步什么

同步包括任务内容、位置、文件名与类型、日期、优先级、置顶、文件夹关系、完成/删除状态和回顾记录。不包括原始文件、浏览器通知授权、手机系统设置、当前搜索/筛选条件及应用程序更新。

同一记录冲突时，按修改时间选取一个整条记录版本，不会逐字段拼接。请保持设备日期时间准确。离线可以继续记录，联网后再合并。

云服务套餐、额度、续期及费用由服务提供方决定，软件不承诺云服务永久免费。旧页面的“免费空间”等提示不代表你实际套餐；请以自己的控制台为准。

## 9. 备份、恢复与更新

### 导出备份

点击“数据” → “导出备份”，保存生成的 JSON 文件到应用之外。备份包含记录、文件夹与回顾等数据，不包括原文件和同步连接配置。密钥应另行安全保存，不放入公开仓库。

升级、批量删除、换手机或清理浏览器数据前都建议备份。以实际生成并能找到 JSON 文件为准；手机若未产生下载文件，不要假定导出成功。

### 导入备份

先另存一份当前数据，再点击“数据” → “导入备份”，选择本应用导出的 JSON。确认后会替换当前设备的本机记录，不是简单追加。

若云同步仍开启，导入后会按正常逻辑再次合并。较新的云端记录或删除状态可能重新覆盖备份中的旧版本；导入并不等于把云端完整回滚。恢复重要数据前，建议停用本机同步，并核实数据后再决定如何与云端合并。

“停用云同步”仅清除本机同步配置，不删除本机记录，也不删除云端数据。不同浏览器、无痕窗口、不同网址及安卓安装版，各自拥有独立的本机存储。清除站点数据、卸载应用等可能丢失尚未备份的本机内容。

### 更新安装版或网页

安卓：备份 → 下载新版 APK → 直接覆盖安装 → 打开检查数据与功能。出现签名冲突或必须卸载的提示时，先停止并反馈，不要为了更新而丢弃旧数据。

网页：继续打开原工作台地址并刷新。不要为了更新代码而清除站点数据；离线缓存更新可能需要关闭该工作台的所有标签页后再次打开。

同步只更新数据，不会把 APK 自动升级到新版。为了让“一句话记录”在两端都保持独立，电脑网页与手机应用都要使用支持此功能的版本。

## 10. 常见问题

| 现象 | 先检查什么 |
| --- | --- |
| 一句话在“全部”里找不到 | 这是独立设计，进入“一句话记录”，或全局搜索后定位 |
| 原来的任务突然少了 | 检查页面、文件夹、搜索、时间筛选、完成状态；不要立即清空或导入覆盖 |
| 手机和电脑内容不同 | 确认同一地址和密钥，先同步更新端，再同步另一端；两端都更新应用代码 |
| 显示“同步密钥不正确” | 检查首尾空格、设备间一致性和服务端配置，不要随意生成新密钥 |
| 显示“Failed to fetch”或连接中断 | 先确认本机记录还在，再检查网络与云函数地址；持续失败需部署者排查服务和跨域配置 |
| 显示“云端同步失败”或额度问题 | 查看完整错误，由部署者检查云函数、数据库及套餐资源；不要反复清数据重试 |
| 找不到拖入的原文件 | 应用没有保存原文件，请根据手动填写的位置到原设备或原软件查找 |
| 没有提示音 | 按第 7 节检查通知设置并实测；网页关闭时不能依赖提醒 |
| 保存后另一端没有变化 | 本机保存和云端同步是两件事；按第 8 节依次同步两端 |

反馈问题时提供：使用的是网页还是安装版、版本、手机/电脑系统、具体操作顺序、完整错误提示。截图要遮住密钥和个人内容；不要上传个人备份或签名证书。

## 11. 一套简单的日常用法

早上：先同步 → 看今日行动 → 自定优先级与时间。

工作中：零碎事情先记一句话；资料登记位置；做完就标记完成。

收工前：整理一句话记录 → 检查未明确位置 → 调整明天计划 → 同步后再换设备。

每周：回顾收集、明确、行动和未来安排，另存一次数据备份。

本版本没有 AI 自动规划、微信/企业微信自动读取、录音或自动生成今日建议。它帮助你记录、查找和整理，优先级与行动决定仍由你掌握。
