# 行动中枢 v1.1.1

发布内容：安卓安装包、完整中文使用说明（Markdown 与离线 HTML）、文件校验清单及整合 ZIP。安卓应用版本为 1.1.1，版本号 111。

## 本版更新

收集箱增加与搜索栏同风格的一句话输入栏。点击“记录”后直接保存到固定的“一句话记录”文件夹，无需先填位置或优先级。该文件夹独立于普通汇总；搜索仍可定位，手动整理移出后进入对应分类。

## 升级须知

先导出备份，再直接覆盖安装。沿用包名 `com.workskill.actionhub`、应用标识 `__UNI__82027B0` 和原签名；不需要卸载旧版，不需要重新生成同步密钥。若出现签名冲突，停止安装并反馈提示，不要先清理数据。

本次更新内嵌界面资源与版本，未修改原生运行时、权限、提醒机制、云函数或个人数据。网页与安装版都需要使用支持新功能的版本；数据同步不能代替更新 APK。

## 验证结果

- 外层和内嵌应用版本一致。
- 新旧包名、应用标识和签名一致。
- v1/v2 签名与 ZIP 对齐验证通过。
- 8 个 dex/so 文件与旧包一致，4 个界面资源与新版源码一致。
- 新包未夹带 keystore 等密钥文件。
- 一句话记录相关 9 项测试通过，原有同步测试通过。
- 未进行安卓真机安装验证；提醒和自动同步的限制见使用说明。

APK 大小：7,668,144 字节。SHA-256：

`a928a70e78044286f6b1a710276070b3583dc50d4e9ba84120daefda44ee28ff`

## 安全说明

旧版 1.1.0 曾误带密码保护的签名证书文件。1.1.1 已排除，原证书移到项目资源目录外，避免再次作为资源打包。为保持覆盖升级兼容，签名未更换。已分发的旧包无法收回，请勿继续转发；从新包移除不能撤销旧包中该文件的既有暴露。

此公开发布不包含旧安装包、个人任务、备份、同步密钥、签名证书、云端私密配置或第三方开发工具。本仓库不是多人共用的数据服务。
