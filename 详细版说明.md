# 行动中枢 · 详细版说明

适用版本：1.1.3。本文只讲安装、保存、同步、备份和维护；任务如何组织，请按自己的习惯探索。

## 选择使用方式

| 使用方式 | 需要准备 | 数据去向 |
| --- | --- | --- |
| 单设备离线使用 | 安卓 APK，或已部署的工作台网址 | 当前应用或浏览器的本机存储 |
| 网盘备份与手动换机 | 百度网盘、夸克网盘或文件管理工具 | 用户手动保存的 JSON 备份文件 |
| 多设备云同步 | 自己的云函数地址、同一同步密钥 | 本机以及自己的 uniCloud 数据库 |

本应用不收软件订阅费。云服务、网页托管、域名和网盘按各平台规则提供，可能有费用或限额；本项目不保证免费无限量服务。网盘备份无需配置 uniCloud，但不能代替跨设备自动合并。

## 安装与更新

从[最新版发布页](https://github.com/xunguangzlj-cloud/action-hub/releases/latest)下载 APK 或完整包 `action-hub-v1.1.3.zip`。完整包含安卓 APK、`web` 网页目录、`uniCloud-aliyun` 同步服务模板，以及本说明和极简版说明。包内不含个人数据、同步密钥或网站专用地址。

### 安卓

1. 下载 `action-hub-android-1.1.3.apk`，打开后按手机提示安装。
2. 更新旧版前先导出备份，选择覆盖更新，不要先卸载或清除数据。
3. 新旧版使用相同包名、应用标识和签名。若提示签名冲突，停止安装并保留旧版数据。
4. 打开后可在“备份”看到网盘入口；旧同步配置继续使用。

### 电脑

用现代浏览器打开部署者提供的工作台 HTTPS 网址；可按浏览器菜单安装为应用或收藏。ZIP 里的 `web` 需先部署到网页托管，GitHub 仓库首页不是工作台。

部署更新时，将新 `web` 目录内的九个文件完整替换到原站点目录。保持原来的域名、访问路径和浏览器配置，可继续访问原本机存储。先导出备份；如果仍显示旧页面，关闭工作台全部窗口后重新打开或刷新，不要先清除网站数据。

### iPhone

用 Safari 打开工作台 HTTPS 网址，点“分享”，或“更多 → 共享”，选择“添加到主屏幕”；如有“作为网页 App 打开”，将其开启。以后从该图标打开。首次联网加载后再尝试离线打开。

这是 Safari 网页应用，没有 iPhone APK 或本次原生 IPA。为避免混淆不同入口的本机存储，建议固定使用主屏幕图标；换入口前先备份，并在新入口确认记录和同步配置。[Apple 官方操作说明](https://support.apple.com/zh-cn/guide/iphone/iphea86e5236/ios)

## 本机保存与离线使用

一句话记录提交、任务表单保存、移动和整理等操作会保存到本机。关闭后重新打开同一应用或同一浏览器入口，应能继续看到记录。

断网期间可以记录，恢复网络后再同步。浏览器无痕模式、清除网站数据、卸载应用或更换浏览器可能导致无法访问原记录，请在操作前备份。网页离线能力需要首次加载和离线缓存安装完成。

## 已有同步服务：连接两台设备

准备以下两项。不要混淆“工作台网址”和“云函数地址”：前者用于打开界面，后者用于交换数据。

- **云函数地址**：部署者提供的完整 HTTPS 接口地址。
- **同步密钥**：与云端配置完全一致的密钥。已有用户沿用原值，不要重新生成。

在每台设备打开“备份”，展开“高级功能 → 多设备云同步”，填写上述两项，点击“保存并测试”，等待“同步完成”。同一手机的安装版和浏览器版也需要分别配置。

### 日常同步顺序

1. 在设备 A 完成记录或修改后，数据先立即保存在本机；停止编辑约一分钟后会自动同步。
2. 如果此时切到后台或息屏，应用会在离开前立即尝试上传；回到前台、恢复网络时，会自动补传仍未完成的修改。
3. 在设备 B 打开应用到前台并联网，应用会拉取云端更新。需要马上确认时，两端仍可点“立即同步”。

同步不是实时推送。手机系统强制结束应用、后台断网或省电策略阻止网络时，离开前的请求仍可能无法完成，但修改不会从本机消失；重新打开应用到前台并联网后会再次同步。换设备前若要求立即可见，以界面显示“同步完成”为准。

尽量不要在两台设备同时修改同一条记录；当前按记录修改时间合并，不是多人协同编辑工具。同步完成会更新任务数据，不会自动更新 APK 或网页程序版本。

### 首次验证

在 A 新建“同步测试”，同步成功；在 B 同步并搜索该文字。随后在 B 修改该条文字、保存、同步，再到 A 同步，确认修改出现。双向成功后即可正常使用。文件夹中看不到时先搜索，避免因当前筛选误判同步失败。

## 首次建立同步服务

已有服务且同步正常的用户跳过本节。第一次部署需要电脑、DCloud 账号、HBuilderX 和本次完整包；可以让熟悉电脑的人帮助完成一次。该模板一个空间只维护一份个人数据，无关用户应各自部署。

### 1. 创建并关联服务空间

登录 [uniCloud 控制台](https://unicloud.dcloud.net.cn/)，创建阿里云服务空间。查看控制台当时的套餐、有效期和限额后选择；已有基础版可继续使用原空间，不要重建。

在 [HBuilderX](https://www.dcloud.io/hbuilderx.html) 登录同一账号，新建启用 uniCloud 阿里云的普通 uni-app 项目，右键 uniCloud 目录关联刚才的空间。将包内 `uniCloud-aliyun/cloudfunctions` 和 `uniCloud-aliyun/database` 合并到项目对应目录，不要多套一层文件夹。[官方快速上手](https://doc.dcloud.net.cn/uniCloud/quickstart)

### 2. 上传数据库结构和云函数

右键 `database/action-hub-state.schema.json` 上传到关联空间。控制台确认有内置数据库集合 `action-hub-state`，Schema 的 `read`、`create`、`update`、`delete` 权限均为 `false`。不需要另购扩展 MongoDB，也无需手工填入任务数据。

右键 `cloudfunctions/action-sync` 上传部署，控制台“云函数/云对象”中应看到普通云函数 `action-sync`。模板使用内置数据库能力和 Node.js `crypto`，不需要额外 AI 密钥、定时触发器或保活任务。运行环境选择支持模板语法的平台可用 Node.js 版本。

### 3. 配置一次密钥和接口地址

在应用“备份 → 高级功能 → 多设备云同步”点“生成强密钥”，再点“复制密钥”。在控制台 `action-sync` 的环境变量中新增：名称 `ACTION_HUB_SYNC_KEY`，值填写完整密钥，然后保存。值中不加引号或变量名前缀。不要把密钥写进代码或公开仓库。

打开该函数的 URL 化或 HTTP 访问配置，路径设为 `/action-sync`；若模板配置已生效，直接复制完整 HTTPS 地址。不要自行拼接空间 ID。该地址是数据接口，直接用浏览器打开不会显示工作台；请用应用“保存并测试”验收。[官方 HTTP 访问说明](https://doc.dcloud.net.cn/uniCloud/http)

### 4. 部署工作台网页

电脑和 iPhone 需要一个可以访问的网页地址。在国内可访问的 HTTPS 静态托管服务中，把 `web` 内以下九个文件上传到同一专用站点目录，首页为 `index.html`：

`index.html`、`app.js`、`styles.css`、`sw.js`、`manifest.webmanifest`、`icon.svg`、`icon-192.png`、`icon-512.png`、`apple-touch-icon.png`。

只上传这些静态资源，不要把数据库、云函数、APK 或密钥上传为网站资源。若选择 uniCloud 前端网页托管，先检查套餐和测试域名限制；长期公开使用需按服务商要求配置正式域名、HTTPS 和相应备案。托管与云函数不是同一个网址。

### 5. 在两端验收

按前文“已有同步服务”配置两台设备。确认地址、密钥与云端环境变量一致，按 A → B、B → A 两个方向验证。遇到跨域问题，检查函数模板的 OPTIONS 响应、跨域响应头和服务商的域名配置，不能以关闭浏览器安全检查代替修复。

## 导出、网盘备份与恢复

打开“备份 → 导出备份”。电脑通常下载 `行动中枢备份-日期.json`；安卓安装版会打开系统分享供选择文件管理或网盘；iPhone 可按 Safari 下载流程保存到“文件”。

点击“导出并打开百度网盘”或“导出并打开夸克网盘”后：

- 支持文件分享的环境会显示系统分享面板，请选择相应网盘并完成保存或上传。
- 浏览器不支持文件分享时，会下载备份并打开相应网盘网页；在网盘选择上传刚下载的 JSON 文件。
- 面板中没有目标网盘时，先安装并登录该网盘，或先保存到文件，再到网盘 App 上传。是否显示网盘由系统和网盘 App 决定。

按钮不会自动登录网盘，也不会绕过网盘确认。系统分享已打开或“备份已导出”不代表云端上传完成；请到网盘确认文件存在。安卓分享无法可靠确认最后保存结果，因此不会仅凭打开面板就刷新备份日期。

备份包含任务、文件夹、位置、备注等记录，不包含拖入文件的原始内容，也不包含同步地址与密钥。文件原件需要你自己保存。每周以及换机、升级、改配置前，建议保存一份新备份。

恢复时，在新设备点“备份 → 导入备份”，选择 JSON 文件，确认后会替换当前设备本机记录，而不是逐条合并。已有数据应先导出。若新设备原本启用了云同步，先停用云同步、导入并核对，再决定连接原云端；重新连接会与原云数据合并，不等于把云端恢复到备份日期。

## 常见问题

| 现象 | 处理 |
| --- | --- |
| 另一设备内容没有变化 | 先同步修改端，成功后再同步接收端；核对同一地址、密钥，使用搜索排除文件夹筛选 |
| 找不到“立即同步” | 先在“备份”的高级功能中配置地址与密钥并保存 |
| 密钥不正确或 401 | 核对云端环境变量和所有设备的密钥；已有服务不要随意重新生成 |
| Failed to fetch | 检查联网、完整 HTTPS 接口地址、空间状态、HTTP 访问和跨域预检；先保留本机数据 |
| 云端同步失败或 500 | 查看对应空间的函数日志，检查内置数据库集合与部署结果 |
| 额度耗尽或等待额度恢复 | 以控制台用量、套餐和错误日志为准；升级套餐不能修复错误地址或密钥 |
| 网页提示 ClientIpNotAllowed | 查看托管测试域名的访问或公网 IP 限制 |
| APK 更新后没有新按钮 | 确认安装 1.1.3；数据同步不能更新应用程序 |
| 保存后息屏，另一台设备仍没看到 | 先让修改端重新回到前台联网，等待“同步完成”；再打开接收端到前台或点“立即同步” |
| 网盘里没有备份 | 回到系统分享或网盘完成上传，并确认文件名；应用不验证网盘上传结果 |
| 网盘按钮在安卓上报错 | 保留本机记录；若已有同步服务，先同步再用电脑导出，否则反馈完整错误与机型，不要卸载 |
| 手机关闭应用后没有提醒 | 网页定时提醒依赖运行状态；安卓受系统通知和后台限制，重要事项另设系统提醒 |

## 版本与验证范围

1.1.3 的主要变化为手机切后台/息屏前立即同步，以及恢复前台后的未完成同步重试。已进行生命周期回归测试、原有功能与模拟双设备同步测试、移动尺寸检查及 APK 签名和资源核对。浏览器模拟不等于所有手机系统的真机后台策略；未完成所有安卓机型、iPhone 真机和真实云端部署验收，安装后请用一条测试记录验证“保存 → 息屏 → 重新打开 → 另一端同步”。

旧版 1.1.0 曾包含受密码保护的签名证书文件，1.1.1 起已排除。为兼容覆盖更新仍沿用原签名；移除不能撤回已分发旧包中的证书。请只从本仓库可信发布入口获取新版。
